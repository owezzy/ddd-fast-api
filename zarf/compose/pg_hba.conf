# Development-only PostgreSQL access policy for Docker Compose.
# Keep password authentication enabled; do not reuse this file for production.
local   all             all                                     trust
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::/0                     scram-sha-256
