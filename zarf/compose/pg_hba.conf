# Development-only PostgreSQL access policy for Docker Compose.
# Trust is intentionally limited to this local development stack; do not reuse
# this file for production.
local   all             all                                     trust
host    all             all             0.0.0.0/0               trust
host    all             all             ::/0                     trust
